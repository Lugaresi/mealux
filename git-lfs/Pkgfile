# Description: Remote terminal application that allows roaming.
# URL:         https://mosh.org/
# Maintainer:  Lugaresi
# Depends on:  go

name=git-lfs
version=3.2.0
release=1
source=(https://github.com/$name/$name/releases/download/v$version/$name-v$version.tar.gz)

build() {
    cd $name-$version
    make
    install -D -m 0755 bin/git-lfs $PKG/usr/bin/git-lfs
}
