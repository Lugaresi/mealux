# Description: libdwarf and dwarfdump utilities.
# URL:         https://www.prevanders.net/dwarf.html
# Maintainer:  Lugaresi
# Depends on:  elfutils

name=libdwarf
version=20180129
release=1
source=(https://www.prevanders.net/$name-$version.tar.gz)

build() {
    cd dwarf-$version
    ./configure --prefix=/usr
    make
    mkdir -p $PKG/usr/bin $PKG/usr/lib $PKG/usr/include
    cp dwarfdump/dwarfdump      $PKG/usr/bin
    cp dwarfdump/dwarfdump.conf $PKG/usr/lib
    cp libdwarf/libdwarf.a      $PKG/usr/lib
    cp libdwarf/libdwarf.h	$PKG/usr/include
}
