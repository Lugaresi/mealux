# Description: Citadel library
# URL:         http://www.citadel.org/
# Maintainer:  Lugaresi
# Depends on:  

name=libcitadel
version=9.17
release=1
source=(http://easyinstall.citadel.org/$name.tar.gz)

build() {
    cd $name
    ./configure --prefix=/usr
    make
    make DESTDIR=$PKG install
}
