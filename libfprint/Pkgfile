# Description: Library with support routines for C such as list, trees, hashes
# URL:         http://www.gtk.org/
# Maintainer:  Juergen Daubert, jue at crux dot nu
# Packager:    Daniel Mueller, daniel at danm dot de
# Depends on: pkg-config, nss, libusb, glib, gtk, meson

name=libfprint
version=0.8.2
release=1
dlver=`echo $version | sed -e s/[.]/_/g`
source=(https://github.com/freedesktop/libfprint/archive/V_$dlver.tar.gz x220.patch)

build () {
    cd libfprint-V_$dlver
#    patch -p1 -i $SRC/x220.patch
    meson --prefix=/usr --buildtype=release -Ddoc=false build && cd build
    ninja
    DESTDIR=$PKG ninja install
    rm -rf $PKG/usr/info
}
