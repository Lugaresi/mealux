# Description: Library with support routines for C such as list, trees, hashes
# URL:         http://www.gtk.org/
# Maintainer:  Juergen Daubert, jue at crux dot nu
# Packager:    Daniel Mueller, daniel at danm dot de
# Depends on:  glib, gtk, libfprint, p5-xml-parser, dbus-glib, linux-pam

name=fprint-demo
version=0.6.0
release=1
source=(http://people.freedesktop.org/~hadess/fprintd-$version.tar.xz fprint)

build () {
    cd fprintd-$version
    ./configure --prefix=/usr --sysconfdir=/usr/etc --libdir=/lib --enable-pam --disable-nls
    make
    make DESTDIR=$PKG install
    rm -rf $PKG/usr/info
    mkdir -p $PKG/etc/rc.d
    cp $SRC/fprint $PKG/etc/rc.d/
    chmod +x $PKG/etc/rc.d/fprint
}
